<div class="jumbotron jumbotron-fluid">
    <div class="container">
        <h1 class="display-4">NPR News Scraper</h1>
        <p class="lead">This is a modified jumbotron that occupies the entire horizontal space of its parent.</p>
    </div>
</div>
<div class="container">
    <nav>
        <div class="nav nav-tabs nav-justified" id="nav-tab" role="tablist">
            <a class="nav-item nav-link active" id="nav-all-tab" data-toggle="tab" href="#nav-all" role="tab"
                aria-controls="nav-all" aria-selected="true">All</a>
            <a class="nav-item nav-link" id="nav-business-tab" data-toggle="tab" href="#nav-business" role="tab"
                aria-controls="nav-business" aria-selected="false">Business</a>
            <a class="nav-item nav-link" id="nav-culture-tab" data-toggle="tab" href="#nav-culture" role="tab"
                aria-controls="nav-culture" aria-selected="false">Culture</a>
            <a class="nav-item nav-link" id="nav-health-tab" data-toggle="tab" href="#nav-health" role="tab"
                aria-controls="nav-health" aria-selected="false">Health</a>
            <a class="nav-item nav-link" id="nav-politics-tab" data-toggle="tab" href="#nav-politics" role="tab"
                aria-controls="nav-politics" aria-selected="false">Politics</a>
            <a class="nav-item nav-link" id="nav-science-tab" data-toggle="tab" href="#nav-science" role="tab"
                aria-controls="nav-science" aria-selected="false">Science</a>
            <a class="nav-item nav-link" id="nav-technology-tab" data-toggle="tab" href="#nav-technology" role="tab"
                aria-controls="nav-technology" aria-selected="false">Technology</a>
            <a class="nav-item nav-link" id="nav-world-tab" data-toggle="tab" href="#nav-world" role="tab"
                aria-controls="nav-world" aria-selected="false">World</a>
        </div>
    </nav>
    <div class="tab-content p-2 mb-5 border-right border-left border-bottom" id="nav-tabContent">
        <div class="tab-pane fade show active" id="nav-all" role="tabpanel" aria-labelledby="nav-all-tab">
            <div class="d-flex justify-content-end pt-4 pr-4 pb-2">
                <button type="button" class="btn btn-danger scrapeButton" id="scrapeNews">Scrape New Articles</button>
            </div>
            <div class="card-columns m-4">
                {{#each all}}
                    {{#if news}}
                    <div class="card">
                        <div class="card-header">
                            <h5><a href="{{link}}">{{title}}</a></h5>
                        </div>
                        <img class="card-img-top" src="{{image}}" alt="{{title}}">
                        <div class="card-body">
                            <p class="card-text">{{summary}}</p>
                            {{!-- <p class="card-text mb-0"><small class="text-muted">{{date}}</small></p> --}}
                        </div>
                        <div class="card-footer d-flex justify-content-between">
                            <button type="button" class="btn btn-outline-secondary saveBtn" value="{{id}}"><i class="fas fa-save"></i> Save</button>
                            <button type="button" class="btn btn-outline-secondary noteBtn" value="{{id}}"><i class="fas fa-edit"></i></button>
                            <a role="button" class="btn btn-outline-secondary" href="{{link}}" target="_blank">NPR <i class="fas fa-link"></i></a>
                        </div>
                    </div>
                    {{/if}}
                {{/each}}
            </div>
        </div>
        <div class="tab-pane fade" id="nav-business" role="tabpanel" aria-labelledby="nav-business-tab">
            <div class="d-flex justify-content-end pt-4 pr-4 pb-2">
                <button type="button" class="btn btn-danger scrapeButton" id="scrapeBusiness">Scrape New Business Articles</button>
            </div>
            <div class="card-columns m-4">
                {{#each all}}
                    {{#if business}}
                    <div class="card">
                        <div class="card-header">
                            <h5><a href="{{link}}">{{title}}</a></h5>
                        </div>
                        <img class="card-img-top" src="{{image}}" alt="{{title}}">
                        <div class="card-body">
                            <p class="card-text">{{summary}}</p>
                        </div>
                        <div class="card-footer d-flex justify-content-between">
                            <button type="button" class="btn btn-outline-secondary saveBtn" value="{{id}}"><i class="fas fa-save"></i> Save</button>
                            <button type="button" class="btn btn-outline-secondary noteBtn" value="{{id}}"><i class="fas fa-edit"></i></button>
                            <a role="button" class="btn btn-outline-secondary" href="{{link}}" target="_blank">NPR <i class="fas fa-link"></i></a>
                        </div>
                    </div>
                    {{/if}}
                {{/each}}
            </div>
        </div>
        <div class="tab-pane fade" id="nav-culture" role="tabpanel" aria-labelledby="nav-culture-tab">
            <div class="d-flex justify-content-end pt-4 pr-4 pb-2">        
                <button type="button" class="btn btn-danger scrapeButton" id="scrapeCulture">Scrape New Culture Articles</button>
            </div>
            <div class="card-columns m-4">
                {{#each all}}
                    {{#if codeswitch}}
                    <div class="card">
                        <div class="card-header">
                            <h5><a href="{{link}}">{{title}}</a></h5>
                        </div>
                        <img class="card-img-top" src="{{image}}" alt="{{title}}">
                        <div class="card-body">
                            <p class="card-text">{{summary}}</p>
                        </div>
                        <div class="card-footer d-flex justify-content-between">
                            <button type="button" class="btn btn-outline-secondary saveBtn" value="{{id}}"><i class="fas fa-save"></i> Save</button>
                            <button type="button" class="btn btn-outline-secondary noteBtn" value="{{id}}"><i class="fas fa-edit"></i></button>
                            <a role="button" class="btn btn-outline-secondary" href="{{link}}" target="_blank">NPR <i class="fas fa-link"></i></a>
                        </div>
                    </div>
                    {{/if}}
                {{/each}}
            </div>
        </div>
        <div class="tab-pane fade" id="nav-health" role="tabpanel" aria-labelledby="nav-health-tab">
            <div class="d-flex justify-content-end pt-4 pr-4 pb-2">        
                <button type="button" class="btn btn-danger scrapeButton" id="scrapeHealth">Scrape New Health Articles</button>
            </div>
            <div class="card-columns m-4">
                {{#each all}}
                    {{#if health}}
                    <div class="card">
                        <div class="card-header">
                            <h5><a href="{{link}}">{{title}}</a></h5>
                        </div>
                        <img class="card-img-top" src="{{image}}" alt="{{title}}">
                        <div class="card-body">
                            <p class="card-text">{{summary}}</p>
                        </div>
                        <div class="card-footer d-flex justify-content-between">
                            <button type="button" class="btn btn-outline-secondary saveBtn" value="{{id}}"><i class="fas fa-save"></i> Save</button>
                            <button type="button" class="btn btn-outline-secondary noteBtn" value="{{id}}"><i class="fas fa-edit"></i></button>
                            <a role="button" class="btn btn-outline-secondary" href="{{link}}" target="_blank">NPR <i class="fas fa-link"></i></a>
                        </div>
                    </div>
                    {{/if}}
                {{/each}}
            </div>
        </div>
        <div class="tab-pane fade" id="nav-politics" role="tabpanel" aria-labelledby="nav-politics-tab">
            <div class="d-flex justify-content-end pt-4 pr-4 pb-2">        
                <button type="button" class="btn btn-danger scrapeButton" id="scrapePolitics">Scrape New Politics Articles</button>
            </div>
            <div class="card-columns m-4">
                {{#each all}}
                    {{#if politics}}
                    <div class="card">
                        <div class="card-header">
                            <h5><a href="{{link}}">{{title}}</a></h5>
                        </div>
                        <img class="card-img-top" src="{{image}}" alt="{{title}}">
                        <div class="card-body">
                            <p class="card-text">{{summary}}</p>
                        </div>
                        <div class="card-footer d-flex justify-content-between">
                            <button type="button" class="btn btn-outline-secondary saveBtn" value="{{id}}"><i class="fas fa-save"></i> Save</button>
                            <button type="button" class="btn btn-outline-secondary noteBtn" value="{{id}}"><i class="fas fa-edit"></i></button>
                            <a role="button" class="btn btn-outline-secondary" href="{{link}}" target="_blank">NPR <i class="fas fa-link"></i></a>
                        </div>
                    </div>
                    {{/if}}
                {{/each}}
            </div>
        </div>
        <div class="tab-pane fade" id="nav-science" role="tabpanel" aria-labelledby="nav-science-tab">
            <div class="d-flex justify-content-end pt-4 pr-4 pb-2">        
                <button type="button" class="btn btn-danger scrapeButton" id="scrapeScience">Scrape New Science Articles</button>
            </div>
            <div class="card-columns m-4">
                {{#each all}}
                    {{#if science}}
                    <div class="card">
                        <div class="card-header">
                            <h5><a href="{{link}}">{{title}}</a></h5>
                        </div>
                        <img class="card-img-top" src="{{image}}" alt="{{title}}">
                        <div class="card-body">
                            <p class="card-text">{{summary}}</p>
                        </div>
                        <div class="card-footer d-flex justify-content-between">
                            <button type="button" class="btn btn-outline-secondary saveBtn" value="{{id}}"><i class="fas fa-save"></i> Save</button>
                            <button type="button" class="btn btn-outline-secondary noteBtn" value="{{id}}"><i class="fas fa-edit"></i></button>
                            <a role="button" class="btn btn-outline-secondary" href="{{link}}" target="_blank">NPR <i class="fas fa-link"></i></a>
                        </div>
                    </div>
                    {{/if}}
                {{/each}}
            </div>
        </div>
        <div class="tab-pane fade" id="nav-technology" role="tabpanel" aria-labelledby="nav-technology-tab">
            <div class="d-flex justify-content-end pt-4 pr-4 pb-2">        
                <button type="button" class="btn btn-danger scrapeButton" id="scrapeTechnology">Scrape New Technology Articles</button>
            </div>
            <div class="card-columns m-4">
                {{#each all}}
                    {{#if technology}}
                    <div class="card">
                        <div class="card-header">
                            <h5><a href="{{link}}">{{title}}</a></h5>
                        </div>
                        <img class="card-img-top" src="{{image}}" alt="{{title}}">
                        <div class="card-body">
                            <p class="card-text">{{summary}}</p>
                        </div>
                        <div class="card-footer d-flex justify-content-between">
                            <button type="button" class="btn btn-outline-secondary saveBtn" value="{{id}}"><i class="fas fa-save"></i> Save</button>
                            <button type="button" class="btn btn-outline-secondary noteBtn" value="{{id}}"><i class="fas fa-edit"></i></button>
                            <a role="button" class="btn btn-outline-secondary" href="{{link}}" target="_blank">NPR <i class="fas fa-link"></i></a>
                        </div>
                    </div>
                    {{/if}}
                {{/each}}
            </div>
        </div>
        <div class="tab-pane fade" id="nav-world" role="tabpanel" aria-labelledby="nav-world-tab">
            <div class="d-flex justify-content-end pt-4 pr-4 pb-2">        
                <button type="button" class="btn btn-danger scrapeButton" id="scrapeWorld">Scrape New World Articles</button>
            </div>
            <div class="card-columns m-4">
                {{#each all}}
                    {{#if world}}
                    <div class="card">
                        <div class="card-header">
                            <h5><a href="{{link}}">{{title}}</a></h5>
                        </div>
                        <img class="card-img-top" src="{{image}}" alt="{{title}}">
                        <div class="card-body">
                            <p class="card-text">{{summary}}</p>
                        </div>
                        <div class="card-footer d-flex justify-content-between">
                            <button type="button" class="btn btn-outline-secondary saveBtn" value="{{id}}"><i class="fas fa-save"></i> Save</button>
                            <button type="button" class="btn btn-outline-secondary noteBtn" value="{{id}}"><i class="fas fa-edit"></i></button>
                            <a role="button" class="btn btn-outline-secondary" href="{{link}}" target="_blank">NPR <i class="fas fa-link"></i></a>
                        </div>
                    </div>
                    {{/if}}
                {{/each}}
            </div>
        </div>
    </div>
</div>